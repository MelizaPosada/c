#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Print numbers
 *
 * Return: 0
 */

int main(void)
{
		int ent;

		for (ent = 0; ent <= 9; ent++)
		printf("%d", ent);
		printf("\n");

		return (0);
}
